# Database (Turso/LibSQL)
# For local development, use a file-based SQLite database
DATABASE_URL="file:local.db"
DATABASE_AUTH_TOKEN=""

# For production, use Turso
# DATABASE_URL="libsql://db-name-user.turso.io"
# DATABASE_AUTH_TOKEN="your-token-here"

# Auth URLs
# APP_URL: Where the SvelteKit app is hosted (auth server)
# API_URL: Where the Durable Object/Worker API is hosted
# PUBLIC_ prefixed vars are available to client-side code
APP_URL="http://localhost:5173"
PUBLIC_APP_URL="http://localhost:5173"
API_URL="http://localhost:8787"
PUBLIC_API_URL="http://localhost:8787"

# Production URLs example:
# APP_URL="https://chat.rizz.zone"
# PUBLIC_APP_URL="https://chat.rizz.zone"
# API_URL="https://chat-api.rizz.zone"
# PUBLIC_API_URL="https://chat-api.rizz.zone"

# Cross-origin cookie domain (optional)
# Set this to share cookies between subdomains
# e.g., ".rizz.zone" allows cookies between chat.rizz.zone and chat-api.rizz.zone
# COOKIE_DOMAIN=".rizz.zone"

# Additional trusted origins (comma-separated, optional)
# TRUSTED_ORIGINS=""

# Better Auth secret (required for production)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
