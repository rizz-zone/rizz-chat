// Currently, this seems kind of pointless, because it is — in the future, it
// will allow for multiple primary views, like one specifically designed for
// image generation
export enum PrimaryViewCategory {
	Chat
}
