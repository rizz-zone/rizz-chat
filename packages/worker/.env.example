# Worker Environment Variables
# These are set in wrangler.jsonc for local dev
# For production, set via: wrangler secret put VAR_NAME

# URL of the SvelteKit auth server (for JWKS verification)
AUTH_URL="http://localhost:5173"

# CORS origin (the SvelteKit app URL)
CORS_ORIGIN="http://localhost:5173"

# Production example:
# AUTH_URL="https://chat.rizz.zone"
# CORS_ORIGIN="https://chat.rizz.zone"
